import { loginStateTypes } from "../../types/loginStateTypes"; 

const loginStore: loginStateTypes = {
  loginData: {
      success: "",
      data: {},
      message: ""
  },
};

export default loginStore;